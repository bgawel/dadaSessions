<div class="row">
  <div class="col-md-12">
     <div class="col-sm-offset-2 col-sm-8">
       <% for(var i=start; i<=stop; ++i) { %>
         <a class="infinite-link">link<%- _.str.pad(i, stop.toString().length, '0') %></a>
       <% }; %>
     </div>
  </div>
</div>

